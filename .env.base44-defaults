# Local placeholder values so the app boots before real credentials are provided.
# Real secrets (delivered via /run/base44/app.env) always override these.

# Cookie/session signing secret (local, not an external service).
JWT_SECRET=base44-dev-cookie-secret-change-me

# Manus platform OAuth / Forge credentials (optional for boot; needed for admin
# login, LLM, image generation, and maps). Override with real values via secrets.
VITE_APP_ID=
VITE_OAUTH_PORTAL_URL=
OAUTH_SERVER_URL=
OWNER_OPEN_ID=
BUILT_IN_FORGE_API_URL=
BUILT_IN_FORGE_API_KEY=
VITE_FRONTEND_FORGE_API_KEY=
VITE_FRONTEND_FORGE_API_URL=
